# My Wallet Extension - 项目文档

## 项目概述

My Wallet 是一个基于 Plasmo 框架开发的多链浏览器扩展钱包，支持 EVM（以太坊及兼容链）、Solana 和 Sui 三大生态。该项目实现了完整的钱包功能，包括账户管理、网络切换、代币管理、转账等核心功能。

**技术栈:**

- Plasmo 0.90.5 (浏览器扩展框架)
- React 18.2.0
- TypeScript 5.3.3
- Ethers.js 6.15.0 (EVM 链交互)
- @solana/web3.js 1.98.4 (Solana 链交互)
- @mysten/sui.js 0.54.1 (Sui 链交互)
- Zustand 5.0.9 (状态管理)
- TailwindCSS 3.4.1 + shadcn/ui (UI 框架)
- Crypto-js 4.2.0 (加密)
- bip39 3.1.0 (助记词生成)

---

## 整体架构

### 1. 扩展架构层次

```
┌─────────────────────────────────────────────────────────────┐
│                        Web Page (MAIN)                       │
│                    window.ccWallet API                       │
└────────────────────────┬────────────────────────────────────┘
                         │ postMessage
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                   Content Script Layer                       │
│              message-bridge.ts (消息桥接)                     │
└────────────────────────┬────────────────────────────────────┘
                         │ chrome.runtime.sendMessage
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                   Background Service Worker                  │
│              background/index.ts (消息处理)                   │
└────────────────────────┬────────────────────────────────────┘
                         │ 访问 walletStore
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                      Wallet Store                            │
│           stores/walletStore.ts (状态管理)                    │
│              chrome.storage.local (持久化)                    │
└─────────────────────────────────────────────────────────────┘
                         ↑
                         │ 读写状态
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                      Popup UI                                │
│              popup.tsx + pages/Index.tsx                     │
└─────────────────────────────────────────────────────────────┘
```

### 2. 核心模块说明

#### 2.1 Popup UI (弹出窗口)

- **入口文件**: [src/popup.tsx](src/popup.tsx)
- **主页面**: [src/pages/Index.tsx](src/pages/Index.tsx)
- **功能**: 提供用户交互界面，根据钱包状态显示不同页面
  - 无账户 → 显示 WalletSetup (创建/导入钱包)
  - 已锁定 → 显示解锁提示
  - 已解锁 → 显示 WalletDashboard (主界面)

#### 2.2 Background Service Worker

- **文件**: [src/background/index.ts](src/background/index.ts)
- **功能**:
  - 监听来自 Content Script 的消息
  - 处理钱包连接、签名、断开等请求
  - 注入钱包脚本到网页 (MAIN world)
  - 管理标签页生命周期

#### 2.3 Content Script

- **文件**: [src/contents/index.tsx](src/contents/index.tsx) (UI 组件)
- **桥接**: [src/contents/message-bridge.ts](src/contents/message-bridge.ts)
- **功能**:
  - 作为网页和扩展之间的消息桥接
  - 转发 postMessage 到 chrome.runtime
  - 返回响应给网页

#### 2.4 Injected Script

- **文件**: [src/background/injected-helper.ts](src/background/injected-helper.ts)
- **功能**:
  - 注入到网页的 MAIN world
  - 暴露 `window.ccWallet` API
  - 提供 connect、disconnect、signMessage、getAccount 方法

#### 2.5 State Management

- **文件**: [src/stores/walletStore.ts](src/stores/walletStore.ts)
- **技术**: Zustand + chrome.storage.local 持久化
- **功能**: 管理钱包全局状态，包括账户、网络、代币等
- **多链支持**: 使用 ChainRecord 结构分别存储 EVM、SOLANA、SUI 的数据

---

## 核心功能

### 1. 钱包管理

#### 1.1 创建钱包

- 生成 12 词助记词 (BIP39)
- 使用 SHA256 加密密码
- 使用 AES 加密助记词和私钥
- 自动创建第一个账户 (HD 路径: m/44'/60'/0'/0/0)

**相关代码**: `walletStore.createWallet()`

#### 1.2 导入钱包

- 支持助记词导入
- 支持私钥导入
- 验证助记词有效性
- 加密存储

**相关代码**: `walletStore.importWallet()`, `walletStore.importPrivateKey()`

#### 1.3 锁定/解锁

- 锁定钱包 (isLocked = true)
- 解锁需要验证密码 (SHA256 哈希比对)

**相关代码**: `walletStore.lockWallet()`, `walletStore.unlockWallet()`

### 2. 账户管理

#### 2.1 创建账户

- 从助记词派生新账户 (HD 路径递增)
- 支持自定义账户名称
- 自动切换到新账户

**相关代码**: `walletStore.createAccount()`

#### 2.2 切换账户

- 切换当前活跃账户
- 更新 UI 显示

**相关代码**: `walletStore.switchAccount()`

#### 2.3 账户信息

- 地址
- 名称
- HD 索引 (index: -1 表示导入的账户)
- 加密的私钥

### 3. 网络管理

#### 3.1 预置网络

**EVM 链:**
- Ethereum Mainnet
- Sepolia Testnet
- Polygon Mainnet
- Polygon Amoy Testnet

**配置**: [src/types/wallet.ts](src/types/wallet.ts) - `DEFAULT_NETWORK`

#### 3.2 网络切换

- 切换当前网络
- 自动更新 RPC Provider

**相关代码**: `walletStore.switchNetwork()`, `walletStore.getProvider()`

#### 3.3 添加自定义网络

- 支持添加新网络
- 配置 RPC URL、Chain ID、符号等

**相关代码**: `walletStore.addNetwork()`

### 4. 代币管理

#### 4.1 支持的代币类型

- ERC20 (同质化代币)
- ERC721 (NFT)
- ERC1155 (多标准代币)

#### 4.2 代币操作

- 添加代币
- 更新余额
- 删除代币

**相关代码**: `walletStore.addToken()`, `walletStore.updateTokenBalance()`, `walletStore.removeToken()`

### 5. 转账功能

#### 5.1 转账流程

1. 输入接收地址
2. 输入转账金额
3. 选择代币 (ETH 或 ERC20)
4. 估算 Gas 费用
5. 确认并发送交易
6. 等待交易确认

**相关组件**: [src/components/wallet/WalletTransfer.tsx](src/components/wallet/WalletTransfer.tsx)

### 6. DApp 交互

#### 6.1 注入 API

网页可通过 `window.ccWallet` 与钱包交互：

```javascript
// 连接钱包
const account = await window.ccWallet.connect()

// 获取当前账户
const account = await window.ccWallet.getAccount()

// 签名消息
const signature = await window.ccWallet.signMessage("Hello World")

// 断开连接
await window.ccWallet.disconnect()
```

#### 6.2 消息流程

```
Web Page (window.ccWallet.connect)
  ↓ postMessage
Content Script (message-bridge)
  ↓ chrome.runtime.sendMessage
Background (处理请求)
  ↓ 访问 walletStore
Background (返回响应)
  ↓ sendResponse
Content Script (转发响应)
  ↓ postMessage
Web Page (Promise resolve)
```

---

## 数据结构

### WalletState

```typescript
interface WalletState {
  isLocked: boolean // 是否锁定
  isConnected: boolean // 是否已连接
  currentChain: ChainType // 当前链类型 (EVM/SOLANA/SUI)
  accountsByChain: ChainRecord<WalletAccount[]> // 按链分类的账户列表
  currentAccountByChain: ChainRecord<WalletAccount | null> // 按链分类的当前账户
  mnemonic: string | null // 加密的助记词
  password: string | null // 加密的密码 (SHA256)
  networksByChain: ChainRecord<WalletNetwork[]> // 按链分类的网络列表
  currentNetworkByChain: ChainRecord<WalletNetwork | null> // 按链分类的当前网络
  tokensByChain: ChainRecord<WalletToken[]> // 按链分类的代币列表
  connectedSitesByChain: ChainRecord<ConnectedSite[]> // 按链分类的已连接网站
}
```

### WalletAccount

```typescript
interface WalletAccount {
  address: string // 账户地址
  name: string // 账户名称
  index: number // HD 索引 (-1 表示导入)
  privateKey: string // 加密的私钥
}
```

### WalletNetwork

```typescript
interface WalletNetwork {
  id: string // 网络 ID
  name: string // 网络名称
  rpcUrl: string // RPC URL
  chainId: number // 链 ID
  symbol: string // 原生代币符号
  blockExplorerUrl?: string // 区块浏览器 URL
}
```

### WalletToken

```typescript
interface WalletToken {
  name: string // 代币名称
  symbol: string // 代币符号
  decimal: number // 小数位数
  address: string // 合约地址
  type: "ERC20" | "ERC721" | "ERC1155" // 代币类型
  tokenId?: string // Token ID (NFT)
  balance?: string // 余额
  image?: string // 图标 URL
}
```

---

## 安全机制

### 1. 加密存储

- **密码**: SHA256 哈希存储
- **助记词**: AES 加密存储
- **私钥**: AES 加密存储
- **存储位置**: chrome.storage.local (浏览器本地存储)

### 2. 权限控制

- **host_permissions**: 访问所有 HTTP/HTTPS 网站
- **permissions**: storage, tabs, scripting, runtime

### 3. 消息验证

- 验证消息来源 (event.source === window)
- 验证消息类型和 requestId
- 超时机制 (30 秒)

---

## UI 组件

### 主要组件

- **WalletSetup**: 钱包初始化 (创建/导入)
- **WalletDashboard**: 主界面 (Tabs 导航)
- **WalletOverview**: 总览页 (余额、代币列表)
- **WalletAccount**: 账户管理页
- **WalletNetwork**: 网络管理页
- **WalletToken**: 代币管理页
- **WalletTransfer**: 转账页
- **AccountDialogs**: 账户相关对话框

### UI 库

- **shadcn/ui**: Button, Card, Dialog, Input, Tabs 等
- **lucide-react**: 图标库
- **sonner**: Toast 通知
- **next-themes**: 暗黑模式支持

---

## 项目结构

```
my-wallet-extension/
├── src/
│   ├── background/           # Background Service Worker
│   │   ├── index.ts         # 主入口
│   │   ├── constants.ts     # 常量定义
│   │   └── injected-helper.ts # 注入脚本
│   ├── contents/            # Content Scripts
│   │   ├── index.tsx        # UI 组件
│   │   └── message-bridge.ts # 消息桥接
│   ├── components/          # React 组件
│   │   ├── ui/             # shadcn/ui 组件
│   │   └── wallet/         # 钱包相关组件
│   ├── stores/             # 状态管理
│   │   └── walletStore.ts  # Zustand Store
│   ├── types/              # TypeScript 类型
│   │   └── wallet.ts       # 钱包类型定义
│   ├── chains/             # 链适配器
│   │   ├── evm.ts          # EVM 链适配器
│   │   ├── solana.ts       # Solana 链适配器
│   │   ├── sui.ts          # Sui 链适配器
│   │   └── index.ts        # 适配器导出
│   ├── hooks/              # React Hooks
│   ├── pages/              # 页面组件
│   │   └── Index.tsx       # 主页面
│   ├── popup.tsx           # Popup 入口
│   └── style.css           # 全局样式
├── package.json            # 项目配置
├── tsconfig.json           # TypeScript 配置
└── README.md               # 项目说明
```

---

## 已实现功能清单

✅ 创建钱包 (助记词生成)
✅ 导入钱包 (助记词/私钥)
✅ 账户管理 (创建、切换、重命名)
✅ 网络管理 (切换、添加自定义网络)
✅ 代币管理 (添加、删除、余额查询)
✅ 转账功能 (ETH 和 ERC20)
✅ 钱包锁定/解锁
✅ DApp 交互 (window.ccWallet API)
✅ 消息签名
✅ 暗黑模式
✅ 多链支持架构 (EVM/Solana/Sui)

---

## ⚠️ 当前项目存在的问题

### 🔴 严重安全问题 (Critical)

#### 1. 暴露的 Infura API 密钥
- **位置**: [src/types/wallet.ts:95](src/types/wallet.ts#L95)
- **问题**: 硬编码的 Infura 项目 ID `c4e28d6f4dfb41cda58ec997065b1068` 公开在源代码中
- **风险**: 任何人都可以使用此密钥，导致速率限制、配额耗尽和潜在滥用
- **影响**: 高 - 这是公开凭证，绝不应提交到代码库

#### 2. 过于宽泛的主机权限
- **位置**: [package.json:59-61](package.json#L59-L61)
- **问题**: `"host_permissions": ["https://*/*", "http://*/*"]` 授予对所有网站的访问权限
- **风险**: 扩展可以在任何网站上注入脚本和访问数据，包括恶意网站
- **影响**: 高 - 违反最小权限原则；应限制为特定域名

#### 3. 未验证的消息桥接通信
- **位置**: [src/contents/message-bridge.ts](src/contents/message-bridge.ts)
- **问题**: 使用 `postMessage(..., "*")` 向所有源广播，没有源验证
- **风险**: 页面上的任何恶意脚本都可以拦截钱包请求和响应
- **影响**: 高 - 允许对钱包操作进行中间人攻击

#### 4. 弱密码哈希
- **位置**: [src/stores/walletStore.ts:288,337,378](src/stores/walletStore.ts#L288)
- **问题**: 使用 SHA256 进行密码哈希，而不是适当的密钥派生函数 (KDF)
- **风险**: SHA256 速度快，容易受到暴力破解攻击；未使用盐值
- **影响**: 高 - 如果存储被泄露，密码可以相对容易地被破解

#### 5. 内存中的私钥暴露
- **位置**: [src/stores/walletStore.ts:718](src/stores/walletStore.ts#L718)
- **问题**: 解密的私钥保存在内存中，没有安全清除
- **风险**: 内存转储或调试工具可能暴露私钥
- **影响**: 严重 - 私钥是钱包的核心资产

#### 6. 缺少速率限制
- **问题**: 没有针对密码/PIN 暴力破解攻击的保护
- **风险**: 攻击者可以尝试无限次密码猜测
- **影响**: 高 - 账户接管风险

#### 7. 缺少种子短语备份验证
- **位置**: [src/components/wallet/form/WalletForm.tsx](src/components/wallet/form/WalletForm.tsx)
- **问题**: 不要求用户验证他们已备份种子短语
- **风险**: 用户如果丢失种子短语将失去对资金的访问权限
- **影响**: 高 - 用户资金损失风险

### 🟠 架构与设计问题 (High)

#### 8. 请求超时处理不足
- **位置**: [src/background/injected-helper.ts:89-92](src/background/injected-helper.ts#L89-L92)
- **问题**: 30 秒超时太长；没有重试逻辑或指数退避
- **风险**: 慢速/挂起的请求阻塞用户交互；没有优雅降级
- **影响**: 中 - 用户体验差和潜在的资源耗尽

#### 9. 请求 ID 冲突预防不足
- **位置**: [src/background/injected-helper.ts:53](src/background/injected-helper.ts#L53)
- **问题**: `generateRequestId()` 使用 `Math.random().toString(36).slice(2, 10)` - 唯一性弱
- **风险**: 冲突可能导致响应被传递到错误的请求
- **影响**: 中 - 并发请求中的竞态条件

#### 10. 缺少输入验证
- **位置**: [src/background/index.ts:35-120](src/background/index.ts#L35-L120)
- **问题**: 消息处理程序在使用前不验证 `message.data` 结构
- **风险**: 格式错误的消息可能导致崩溃或意外行为
- **影响**: 中 - 拒绝服务潜力

#### 11. 硬编码链假设
- **位置**: [src/components/wallet/WalletToken.tsx:34,38](src/components/wallet/WalletToken.tsx#L34)
- **问题**: 代币管理硬编码为仅 EVM，但钱包支持多链
- **风险**: 用户体验不一致；Solana/Sui 代币无法管理
- **影响**: 中 - 功能不完整

#### 12. 未受保护的消息监听器
- **位置**: [src/background/index.ts:16](src/background/index.ts#L16)
- **问题**: `chrome.runtime.onMessage.addListener()` 不验证发送者
- **风险**: 任何扩展或内容脚本都可以向后台发送消息
- **影响**: 中 - 未经授权访问钱包功能

### 🟡 错误处理与日志问题 (Medium)

#### 13. 静默错误吞没
- **位置**: [src/components/wallet/WalletToken.tsx:81-83,169-171](src/components/wallet/WalletToken.tsx#L81-L83)
- **问题**: `catch { // ignore individual token errors }` - 错误被静默忽略
- **风险**: 用户不知道代币余额更新失败；调试困难
- **影响**: 中 - 可观察性差

#### 14. 错误上下文不足
- **位置**: [src/background/index.ts:31](src/background/index.ts#L31)
- **问题**: `error?.message || String(error)` 不捕获堆栈跟踪或错误类型
- **风险**: 难以诊断生产环境中的问题
- **影响**: 低-中 - 调试挑战

#### 15. 生产环境中的控制台日志
- **位置**: 多个文件 (background/index.ts, hooks/useWalletBalance.tsx 等)
- **问题**: `console.error()` 调用保留在生产代码中
- **风险**: 可能记录敏感信息；性能影响
- **影响**: 低 - 轻微安全/性能问题

### 🟢 代码质量与可维护性 (Low-Medium)

#### 16. 重复的密码验证逻辑
- **位置**: AccountDialogs.tsx:38-39, WalletTransfer.tsx
- **问题**: 密码验证在组件间重复，而不是集中化
- **风险**: 验证不一致；维护负担
- **影响**: 低 - 技术债务

#### 17. 类型安全问题
- **位置**: [src/background/index.ts:26](src/background/index.ts#L26)
- **问题**: `handler: () => Promise<any>` 使用 `any` 类型
- **风险**: 失去类型安全；更难捕获错误
- **影响**: 低 - 类型安全问题

#### 18. 缺少空值检查
- **位置**: [src/stores/walletStore.ts:718](src/stores/walletStore.ts#L718)
- **问题**: `state.password as string` - 强制转换而不进行空值检查
- **风险**: 如果密码为空，则运行时错误
- **影响**: 低 - 潜在崩溃

#### 19. 缺少单元测试
- **问题**: 项目中没有测试文件 (*.test.ts, *.spec.ts)
- **风险**: 无法验证代码正确性；重构困难
- **影响**: 中 - 质量保证问题

### ⚡ 性能瓶颈 (Medium)

#### 20. 低效的代币余额更新
- **位置**: [src/components/wallet/WalletToken.tsx:55-90](src/components/wallet/WalletToken.tsx#L55-L90)
- **问题**: 在每次组件挂载/更新时获取所有代币的余额
- **风险**: 过多的 RPC 调用；UI 缓慢；速率限制
- **影响**: 中 - 多代币时的可扩展性问题

#### 21. 缺少缓存策略
- **位置**: [src/stores/walletStore.ts:551](src/stores/walletStore.ts#L551)
- **问题**: 每次调用都创建 `new JsonRpcProvider()`，没有缓存
- **风险**: 重复的提供者实例化；内存泄漏
- **影响**: 中 - 资源效率低下

### 🔒 缺少的安全功能

#### 22. 缺少会话管理
- **问题**: 锁定钱包状态没有超时；没有活动监控
- **风险**: 如果扩展保持打开，钱包可能无限期保持解锁状态
- **影响**: 中 - 物理安全风险

#### 23. 缺少审计日志
- **问题**: 没有钱包操作记录（转账、账户创建等）
- **风险**: 无法检测未经授权的访问或可疑活动
- **影响**: 中 - 取证/合规问题

#### 24. 缺少 CSRF 保护
- **问题**: 敏感操作没有状态验证
- **风险**: 恶意网站可能触发钱包操作
- **影响**: 中 - 未经授权的交易

#### 25. 弱随机数生成
- **位置**: [src/background/injected-helper.ts:53](src/background/injected-helper.ts#L53)
- **问题**: `Math.random()` 不是加密安全的
- **风险**: 可预测的请求 ID 可能被利用
- **影响**: 低-中 - 加密弱点

---

## 🚀 改进建议与优化方案

### 立即行动项 (P0 - 必须修复)

1. **移除并轮换 Infura API 密钥**
   - 从代码中删除硬编码的 API 密钥
   - 使用环境变量或用户配置的 RPC 端点
   - 轮换已暴露的密钥

2. **限制主机权限**
   - 将 `host_permissions` 限制为特定域名或使用 `activeTab` 权限
   - 实现权限请求流程

3. **实现适当的密码哈希**
   - 使用 PBKDF2、bcrypt 或 Argon2 替代 SHA256
   - 添加盐值和多次迭代
   - 示例: `crypto.subtle.deriveKey()` with PBKDF2

4. **添加消息桥接的源验证**
   ```typescript
   // 在 message-bridge.ts 中
   const ALLOWED_ORIGINS = ['https://yourdapp.com']
   if (!ALLOWED_ORIGINS.includes(event.origin)) return
   ```

5. **实现发送者验证**
   ```typescript
   // 在 background/index.ts 中
   chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
     if (!sender.tab || !sender.url) return false
     // 验证发送者
   })
   ```

6. **添加速率限制**
   - 实现密码尝试计数器
   - 在 N 次失败尝试后锁定（例如 5 次）
   - 添加指数退避

7. **添加种子短语备份验证**
   - 要求用户按顺序输入随机单词
   - 在允许继续之前验证

### 短期改进 (P1 - 1-2 周)

8. **实现输入验证**
   - 使用 Zod 模式验证所有消息数据
   - 添加地址格式验证
   - 验证金额和 gas 参数

9. **改进错误处理**
   - 创建集中式错误处理服务
   - 添加错误边界组件
   - 实现结构化日志记录（不在生产环境中使用 console.log）

10. **优化代币余额获取**
    - 实现批量余额查询
    - 添加缓存层（5-10 分钟 TTL）
    - 使用 Web Workers 进行后台更新

11. **实现 Provider 缓存**
    ```typescript
    private providerCache = new Map<string, JsonRpcProvider>()

    getProvider(chain?: ChainType): JsonRpcProvider | null {
      const key = `${chain}-${currentNetwork.rpcUrl}`
      if (!this.providerCache.has(key)) {
        this.providerCache.set(key, new JsonRpcProvider(rpcUrl))
      }
      return this.providerCache.get(key)
    }
    ```

12. **添加请求重试逻辑**
    - 实现指数退避
    - 最多重试 3 次
    - 为不同错误类型添加不同的重试策略

13. **改进请求 ID 生成**
    ```typescript
    function generateRequestId(): string {
      return `${Date.now()}-${crypto.randomUUID()}`
    }
    ```

14. **实现会话超时**
    - 15 分钟不活动后自动锁定
    - 添加"保持解锁"选项（带警告）
    - 监控用户活动

### 中期改进 (P2 - 1-2 月)

15. **添加单元测试**
    - 使用 Vitest 为核心功能编写测试
    - 目标覆盖率 > 80%
    - 重点测试：加密、钱包操作、消息处理

16. **实现审计日志**
    - 记录所有敏感操作
    - 存储时间戳、操作类型、结果
    - 提供日志查看 UI

17. **添加交易历史**
    - 存储已发送的交易
    - 显示待处理/已确认状态
    - 与区块浏览器集成

18. **实现多链代币管理**
    - 为 Solana 添加 SPL 代币支持
    - 为 Sui 添加 Coin 支持
    - 统一代币管理 UI

19. **添加 Gas 费用优化**
    - 显示慢/标准/快速 gas 选项
    - 集成 gas 价格预言机
    - 允许自定义 gas 设置

20. **实现地址簿**
    - 保存常用地址
    - 添加标签和备注
    - 支持导入/导出

21. **添加交易模拟**
    - 在发送前模拟交易
    - 显示预期结果和错误
    - 防止失败的交易

### 长期改进 (P3 - 3-6 月)

22. **硬件钱包支持**
    - 集成 Ledger/Trezor
    - 支持硬件签名
    - 添加硬件钱包检测

23. **多语言支持 (i18n)**
    - 使用 react-i18next
    - 支持英语、中文、日语等
    - 自动语言检测

24. **NFT 展示**
    - NFT 画廊视图
    - 元数据获取和缓存
    - NFT 转移功能

25. **批量转账**
    - 支持一次发送到多个地址
    - CSV 导入功能
    - 批量交易确认

26. **交易加速/取消**
    - 允许用更高的 gas 替换待处理交易
    - 实现交易取消（gas 价格提升）
    - 显示替换状态

27. **WalletConnect 集成**
    - 支持 WalletConnect v2
    - 扫描二维码连接
    - 管理活跃会话

28. **Swap 功能**
    - 集成 DEX 聚合器（1inch、Jupiter）
    - 显示最佳路由和价格
    - 滑点保护

29. **Staking 支持**
    - 显示 staking 机会
    - 一键 stake/unstake
    - 显示奖励和 APY

30. **安全审计**
    - 聘请第三方安全公司审计
    - 修复所有发现的漏洞
    - 发布审计报告

### 架构优化建议

#### 1. 状态管理优化
- 考虑将 Zustand 拆分为多个 store（账户、网络、代币）
- 实现选择器以避免不必要的重新渲染
- 添加状态持久化版本控制和迁移

#### 2. 代码组织
- 按功能而不是类型组织文件（features/accounts, features/tokens）
- 创建共享的实用程序库
- 提取常量到配置文件

#### 3. 性能优化
- 实现虚拟滚动用于长列表
- 使用 React.memo 和 useMemo 优化渲染
- 延迟加载非关键组件

#### 4. 安全最佳实践
- 实现内容安全策略 (CSP)
- 添加子资源完整性 (SRI)
- 使用安全的随机数生成器 (crypto.getRandomValues)
- 实现私钥的安全内存清除

#### 5. 用户体验改进
- 添加加载骨架屏
- 实现乐观 UI 更新
- 添加更好的错误消息和恢复选项
- 实现撤销/重做功能

---

## 开发指南

### 启动开发服务器

```bash
pnpm dev
```

### 构建生产版本

```bash
pnpm build
```

### 运行测试

```bash
pnpm test
```

### 加载扩展

1. 打开 Chrome 扩展管理页面 (`chrome://extensions/`)
2. 启用"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `build/chrome-mv3-dev` 目录

---

## 技术亮点

1. **Plasmo 框架**: 简化浏览器扩展开发，支持热重载
2. **Zustand 持久化**: 使用 chrome.storage.local 实现状态持久化
3. **消息桥接**: 实现 Web Page ↔ Content Script ↔ Background 的完整通信链路
4. **MAIN World 注入**: 通过 chrome.scripting.executeScript 注入到页面主世界
5. **加密安全**: 使用 AES 加密敏感数据，SHA256 哈希密码（需改进）
6. **HD 钱包**: 支持 BIP44 标准的分层确定性钱包
7. **TypeScript**: 完整的类型定义，提高代码可维护性
8. **多链架构**: 使用 ChainRecord 和适配器模式支持多链

---

## 注意事项

1. **⚠️ 安全警告**: 此项目仅供学习和测试使用，**不建议用于生产环境存储大额资产**
2. **🔑 助记词备份**: 用户必须妥善保管助记词，丢失后无法恢复
3. **🔒 密码管理**: 密码用于加密本地数据，忘记密码需要重新导入钱包
4. **🌐 网络安全**: 使用公共 RPC 节点可能存在隐私风险，建议使用自己的节点
5. **🔍 代码审计**: 生产使用前需要进行完整的安全审计
6. **🚨 已知漏洞**: 请参阅上方"当前项目存在的问题"部分，了解已知的安全和架构问题

---

## 贡献指南

### 提交代码前

1. 确保代码通过 TypeScript 类型检查
2. 运行测试套件（待实现）
3. 遵循现有的代码风格
4. 更新相关文档

### 安全问题报告

如果发现安全漏洞，请**不要**公开提交 issue，而是直接联系项目维护者。

---

## 联系方式

- **作者**: cc
- **版本**: 0.0.1
- **框架**: Plasmo
- **许可**: MIT (假设)

---

**最后更新**: 2026-03-01
**文档版本**: 2.0 (包含问题分析和改进建议)