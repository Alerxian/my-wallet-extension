# My Wallet Extension - 项目文档

## 项目概述

My Wallet 是一个基于 Plasmo 框架开发的浏览器扩展钱包，支持以太坊及其兼容链。该项目实现了完整的钱包功能，包括账户管理、网络切换、代币管理、转账等核心功能。

**技术栈：**
- Plasmo 0.90.5 (浏览器扩展框架)
- React 18.2.0
- TypeScript 5.3.3
- Ethers.js 6.15.0 (以太坊交互)
- Zustand 5.0.9 (状态管理)
- TailwindCSS 3.4.1 + shadcn/ui (UI 框架)
- Crypto-js 4.2.0 (加密)

---

## 整体架构

### 1. 扩展架构层次

```
┌─────────────────────────────────────────────────────────────┐
│                        Web Page (MAIN)                       │
│                    window.ccWallet API                       │
└────────────────────────┬────────────────────────────────────┘
                         │ postMessage
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                   Content Script Layer                       │
│              message-bridge.ts (消息桥接)                     │
└────────────────────────┬────────────────────────────────────┘
                         │ chrome.runtime.sendMessage
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                   Background Service Worker                  │
│              background/index.ts (消息处理)                   │
└────────────────────────┬────────────────────────────────────┘
                         │ 访问 walletStore
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                      Wallet Store                            │
│           stores/walletStore.ts (状态管理)                    │
│              chrome.storage.local (持久化)                    │
└─────────────────────────────────────────────────────────────┘
                         ↑
                         │ 读写状态
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                      Popup UI                                │
│              popup.tsx + pages/Index.tsx                     │
└─────────────────────────────────────────────────────────────┘
```

### 2. 核心模块说明

#### 2.1 Popup UI (弹出窗口)
- **入口文件**: `src/popup.tsx`
- **主页面**: `src/pages/Index.tsx`
- **功能**: 提供用户交互界面，根据钱包状态显示不同页面
  - 无账户 → 显示 WalletSetup (创建/导入钱包)
  - 已锁定 → 显示解锁提示
  - 已解锁 → 显示 WalletDashboard (主界面)

#### 2.2 Background Service Worker
- **文件**: `src/background/index.ts`
- **功能**:
  - 监听来自 Content Script 的消息
  - 处理钱包连接、签名、断开等请求
  - 注入钱包脚本到网页 (MAIN world)
  - 管理标签页生命周期

#### 2.3 Content Script
- **文件**: `src/contents/index.tsx` (UI 组件)
- **桥接**: `src/contents/message-bridge.ts`
- **功能**:
  - 作为网页和扩展之间的消息桥接
  - 转发 postMessage 到 chrome.runtime
  - 返回响应给网页

#### 2.4 Injected Script
- **文件**: `src/background/injected-helper.ts`
- **功能**:
  - 注入到网页的 MAIN world
  - 暴露 `window.ccWallet` API
  - 提供 connect、disconnect、signMessage、getAccount 方法

#### 2.5 State Management
- **文件**: `src/stores/walletStore.ts`
- **技术**: Zustand + chrome.storage.local 持久化
- **功能**: 管理钱包全局状态，包括账户、网络、代币等

---

## 核心功能

### 1. 钱包管理

#### 1.1 创建钱包
- 生成 12 词助记词 (BIP39)
- 使用 SHA256 加密密码
- 使用 AES 加密助记词和私钥
- 自动创建第一个账户 (HD 路径: m/44'/60'/0'/0/0)

**相关代码**: `walletStore.createWallet()`

#### 1.2 导入钱包
- 支持助记词导入
- 支持私钥导入
- 验证助记词有效性
- 加密存储

**相关代码**: `walletStore.importWallet()`, `walletStore.importPrivateKey()`

#### 1.3 锁定/解锁
- 锁定钱包 (isLocked = true)
- 解锁需要验证密码 (SHA256 哈希比对)

**相关代码**: `walletStore.lockWallet()`, `walletStore.unlockWallet()`

### 2. 账户管理

#### 2.1 创建账户
- 从助记词派生新账户 (HD 路径递增)
- 支持自定义账户名称
- 自动切换到新账户

**相关代码**: `walletStore.createAccount()`

#### 2.2 切换账户
- 切换当前活跃账户
- 更新 UI 显示

**相关代码**: `walletStore.switchAccount()`

#### 2.3 账户信息
- 地址
- 名称
- HD 索引 (index: -1 表示导入的账户)
- 加密的私钥

### 3. 网络管理

#### 3.1 预置网络
- Ethereum Mainnet
- Sepolia Testnet
- Polygon Mainnet
- Polygon Amoy Testnet

**配置**: `src/types/wallet.ts` - `DEFAULT_NETWORK`

#### 3.2 网络切换
- 切换当前网络
- 自动更新 RPC Provider

**相关代码**: `walletStore.switchNetwork()`, `walletStore.getProvider()`

#### 3.3 添加自定义网络
- 支持添加新网络
- 配置 RPC URL、Chain ID、符号等

**相关代码**: `walletStore.addNetwork()`

### 4. 代币管理

#### 4.1 支持的代币类型
- ERC20 (同质化代币)
- ERC721 (NFT)
- ERC1155 (多标准代币)

#### 4.2 代币操作
- 添加代币
- 更新余额
- 删除代币

**相关代码**: `walletStore.addToken()`, `walletStore.updateTokenBalance()`, `walletStore.removeToken()`

### 5. 转账功能

#### 5.1 转账流程
1. 输入接收地址
2. 输入转账金额
3. 选择代币 (ETH 或 ERC20)
4. 估算 Gas 费用
5. 确认并发送交易
6. 等待交易确认

**相关组件**: `src/components/wallet/WalletTransfer.tsx`

### 6. DApp 交互

#### 6.1 注入 API
网页可通过 `window.ccWallet` 与钱包交互：

```javascript
// 连接钱包
const account = await window.ccWallet.connect()

// 获取当前账户
const account = await window.ccWallet.getAccount()

// 签名消息
const signature = await window.ccWallet.signMessage("Hello World")

// 断开连接
await window.ccWallet.disconnect()
```

#### 6.2 消息流程
```
Web Page (window.ccWallet.connect)
  ↓ postMessage
Content Script (message-bridge)
  ↓ chrome.runtime.sendMessage
Background (处理请求)
  ↓ 访问 walletStore
Background (返回响应)
  ↓ sendResponse
Content Script (转发响应)
  ↓ postMessage
Web Page (Promise resolve)
```

---

## 数据结构

### WalletState
```typescript
interface WalletState {
  isLocked: boolean              // 是否锁定
  isConnected: boolean           // 是否已连接
  accounts: WalletAccount[]      // 账户列表
  currentAccount: WalletAccount  // 当前账户
  mnemonic: string               // 加密的助记词
  password: string               // 加密的密码 (SHA256)
  networks: WalletNetwork[]      // 网络列表
  currentNetwork: WalletNetwork  // 当前网络
  token: WalletToken[]           // 代币列表
  connectedSites: ConnectedSite[] // 已连接的网站
}
```

### WalletAccount
```typescript
interface WalletAccount {
  address: string      // 账户地址
  name: string         // 账户名称
  index: number        // HD 索引 (-1 表示导入)
  privateKey: string   // 加密的私钥
}
```

### WalletNetwork
```typescript
interface WalletNetwork {
  id: string                  // 网络 ID
  name: string                // 网络名称
  rpcUrl: string              // RPC URL
  chainId: number             // 链 ID
  symbol: string              // 原生代币符号
  blockExplorerUrl?: string   // 区块浏览器 URL
}
```

### WalletToken
```typescript
interface WalletToken {
  name: string                           // 代币名称
  symbol: string                         // 代币符号
  decimal: number                        // 小数位数
  address: string                        // 合约地址
  type: "ERC20" | "ERC721" | "ERC1155"  // 代币类型
  tokenId?: string                       // Token ID (NFT)
  balance?: string                       // 余额
  image?: string                         // 图标 URL
}
```

---

## 安全机制

### 1. 加密存储
- **密码**: SHA256 哈希存储
- **助记词**: AES 加密存储
- **私钥**: AES 加密存储
- **存储位置**: chrome.storage.local (浏览器本地存储)

### 2. 权限控制
- **host_permissions**: 访问所有 HTTP/HTTPS 网站
- **permissions**: storage, tabs, scripting, runtime

### 3. 消息验证
- 验证消息来源 (event.source === window)
- 验证消息类型和 requestId
- 超时机制 (30 秒)

---

## UI 组件

### 主要组件
- **WalletSetup**: 钱包初始化 (创建/导入)
- **WalletDashboard**: 主界面 (Tabs 导航)
- **WalletOverview**: 总览页 (余额、代币列表)
- **WalletAccount**: 账户管理页
- **WalletNetwork**: 网络管理页
- **WalletToken**: 代币管理页
- **WalletTransfer**: 转账页
- **AccountDialogs**: 账户相关对话框

### UI 库
- **shadcn/ui**: Button, Card, Dialog, Input, Tabs 等
- **lucide-react**: 图标库
- **sonner**: Toast 通知

---

## 开发指南

### 启动开发服务器
```bash
pnpm dev
```

### 构建生产版本
```bash
pnpm build
```

### 加载扩展
1. 打开 Chrome 扩展管理页面 (`chrome://extensions/`)
2. 启用"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `build/chrome-mv3-dev` 目录

---

## 项目结构

```
my-wallet-extension/
├── src/
│   ├── background/           # Background Service Worker
│   │   ├── index.ts         # 主入口
│   │   ├── constants.ts     # 常量定义
│   │   └── injected-helper.ts # 注入脚本
│   ├── contents/            # Content Scripts
│   │   ├── index.tsx        # UI 组件
│   │   └── message-bridge.ts # 消息桥接
│   ├── components/          # React 组件
│   │   ├── ui/             # shadcn/ui 组件
│   │   └── wallet/         # 钱包相关组件
│   ├── stores/             # 状态管理
│   │   └── walletStore.ts  # Zustand Store
│   ├── types/              # TypeScript 类型
│   │   └── wallet.ts       # 钱包类型定义
│   ├── hooks/              # React Hooks
│   ├── pages/              # 页面组件
│   │   └── Index.tsx       # 主页面
│   ├── popup.tsx           # Popup 入口
│   └── style.css           # 全局样式
├── package.json            # 项目配置
├── tsconfig.json           # TypeScript 配置
└── README.md               # 项目说明
```

---

## 已实现功能清单

✅ 创建钱包 (助记词生成)
✅ 导入钱包 (助记词/私钥)
✅ 账户管理 (创建、切换、重命名)
✅ 网络管理 (切换、添加自定义网络)
✅ 代币管理 (添加、删除、余额查询)
✅ 转账功能 (ETH 和 ERC20)
✅ 钱包锁定/解锁
✅ DApp 交互 (window.ccWallet API)
✅ 消息签名
✅ 暗黑模式

---

## 待优化功能

🔲 交易历史记录
🔲 Gas 费用优化建议
🔲 多语言支持
🔲 NFT 展示
🔲 硬件钱包支持
🔲 批量转账
🔲 地址簿管理
🔲 交易加速/取消

---

## 注意事项

1. **安全警告**: 此项目仅供学习和测试使用，不建议用于生产环境存储大额资产
2. **助记词备份**: 用户必须妥善保管助记词，丢失后无法恢复
3. **密码管理**: 密码用于加密本地数据，忘记密码需要重新导入钱包
4. **网络安全**: 使用公共 RPC 节点可能存在隐私风险，建议使用自己的节点
5. **代码审计**: 生产使用前需要进行完整的安全审计

---

## 技术亮点

1. **Plasmo 框架**: 简化浏览器扩展开发，支持热重载
2. **Zustand 持久化**: 使用 chrome.storage.local 实现状态持久化
3. **消息桥接**: 实现 Web Page ↔ Content Script ↔ Background 的完整通信链路
4. **MAIN World 注入**: 通过 chrome.scripting.executeScript 注入到页面主世界
5. **加密安全**: 使用 AES 加密敏感数据，SHA256 哈希密码
6. **HD 钱包**: 支持 BIP44 标准的分层确定性钱包
7. **TypeScript**: 完整的类型定义，提高代码可维护性

---

## 联系方式

- **作者**: cc
- **版本**: 0.0.1
- **框架**: Plasmo
- **许可**: MIT (假设)

---

**最后更新**: 2026-03-01
